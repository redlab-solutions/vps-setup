# Hermes manages its own user-level systemd service (hermes-gateway.service).
# To start/stop: use `hermes gateway start` or `systemctl --user start hermes-gateway`
# This file is kept for reference only — do NOT install as system service.
#
# Hermes auto-creates: ~/.config/systemd/user/hermes-gateway.service
# Check status: systemctl --user status hermes-gateway
